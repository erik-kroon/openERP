# Versioned Takumi v2: synthetic Worker and long-page evidence

Observed 2026-09-24 on fresh disposable PostgreSQL 17 databases with a local Cloudflare Worker. This does **not** activate an actual-company invoice policy or prove delivery. See the [v1 Worker recipe](wave2-legal-pdf-worker.md) and [`apps/api/INVOICE-PDF.md`](../../../apps/api/docs/INVOICE-PDF.md) for the three-operator synthetic issue fixture and renderer-only long-page reproduction steps. No test file, provider call or real credential was added.

## Repeat

1. On a disposable database, apply the versioned SQL through [`8600-ar-legal-pdf-v2.sql`](../../../apps/api/migrations/8600-ar-legal-pdf-v2.sql) (observed: `139` migrations, including independent 83xx–85xx slices). Provision a synthetic book, make a reviewed one-line domestic 25% accrual issue using the retained source flow, and start the local Worker with a restricted role and temp `.dev.vars` outside the checkout.
2. With that issue's saved `id` and `digest`, `POST /api/v1/entities/entity_synthetic/books/book_synthetic/commerce/legal-invoice-pdfs` using a fresh `idempotency-key`, the scoped bearer token and JSON `{"issueId":"<saved-id>","issueDigest":"<saved-digest>","rendererVersion":"openerp-se-invoice-takumi-v2"}`. Decode `artifact.contentBase64`; verify `%PDF-`, exact byte length and SHA-256. Repeat with the **same key**, GET `/legal-invoice-pdfs/<capture-id>`, and POST `/legal-invoice-pdfs/<capture-id>/render`; all return identical capture/artifact bytes. A different-key v1 request for this same issue must return `409 IdempotencyConflict` rather than replacing bytes.
3. On a second disposable database with the same migrations, make the same synthetic source issue, but request `openerp-se-invoice-takumi-v1`. Compare the sealed v1 bytes with [the pre-v2 v1 PDF](wave2-legal-pdf-worker/issued-AR-1.pdf). Repeat and resume with the v1 capture. Never ask v2 to re-render a retained v1 capture.
4. Run the documented renderer-only 50-line and tall-description probes in `apps/api/INVOICE-PDF.md`. Inspect all page images and extracted text for one description with its own detail and amount per row, one payable total, and no footer-only page. Stop and delete local Worker/database processes and credentials afterward.

## Observed

- The Worker returned `200` with a sealed v2 `AR-1.pdf` of `10047` bytes, SHA-256 `f092595b7ddfcda7cebae8a4c87c15deb05f2804cf6d265721ceaf3727fc35ee`. The descriptor and immutable capture both said `openerp-se-invoice-takumi-v2`; PDF hash/length matched the actual bytes; `legalInvoice=true`, `delivered=false`. Same-key retry, GET and resume returned identical JSON/PDF. One PDF capture and one artifact row remained. Different-key v1 on that issue returned `409 IdempotencyConflict`.
- The second database returned v1 `AR-1.pdf` of `9997` bytes, SHA-256 `15fd8f0e97cc8e5524638d19c091bb0f370e2bb40adcd15afbc4ad791c18c15d`, **byte-for-byte equal** to the pre-v2 v1 evidence PDF. Its descriptor stayed v1. Same-key retry and resume returned identical records. The 8600 migration SHA-256 is `2afecc4cda42720fba2bc38c77ef33807c05b7c91b054f738f47f8f774f51936`; it replaces approved SQL functions and leaves the v1 capture rows/sealed bytes untouched.
- The [real Worker v2 PDF](wave2-legal-pdf-worker/issued-AR-1-v2.pdf) and [white PNG](wave2-legal-pdf-worker/issued-AR-1-v2.png) were visually inspected at 595×842. The synthetic issue date, parties, line amount, VAT, `125,00 kr` payable and footer are readable.
- The separate [50-line v2 stress PDF](wave2-legal-pdf-worker/v2-50-two-words.pdf) has 50 paired line/detail blocks in five pages; [page 5](wave2-legal-pdf-worker/v2-50-two-words-page-5.png) contains the full total and terms, rather than a footer alone. A 20-word×50 fixture yielded 26 pages with 50 paired blocks and no footer-only page. A single extraordinarily tall line, repeated column headings on continuation pages, screen-reader tagging and deployed Worker resource limits remain open.

This is synthetic, local application and rendering evidence. No bank/provider acceptance, email/Peppol send, actual tenant approval or statutory sign-off is inferred.
