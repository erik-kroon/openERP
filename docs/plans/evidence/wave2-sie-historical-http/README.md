# Synthetic SIE historical-import HTTP proof

Observed on 2026-09-24 at `2a0fa09` through a local Wrangler Worker and fresh disposable PostgreSQL 17. The Worker used restricted `openerp_app`; migrations were applied through `8380-sie-proposal-posting-guard.sql`, excluding concurrent uncommitted banking migration `8720`. All data was synthetic. The Worker and database were stopped/deleted; access credentials are not in these token-free response receipts. The ad hoc driver scripts remain untracked under `/tmp/openerp-sie-e2e-n5pygtgu/`; they were **not** added as repository tests. Its combined scenario driver was not rerun after expectation edits, so this record does not claim that one script is validated end-to-end.

The source was a bounded PC8 SIE4 sample with opening account controls `1930=0.00`, `2999=0.00`, closing controls `1930=100.00`, `2999=-100.00`, and one balanced two-line `#VER`/`#TRANS` voucher. Independently supplied controls, source bytes and scoped tokens were required. The [financial response receipts](financial-result.json) contain 26 labeled HTTP requests/statuses: source/preview/inventory/plan/chunk/stage replays, full-history basis, financial proposal/approval, owner-run posting at sequence 1, and closing comparison with `postingComplete=true`, `balanced=true`, two zero-difference controls. They also show unauthorized 401, changed bytes 409, wrong independent closing 422, stale fence 409, and direct execution while paused 409. The [opening-recovery receipts](recovery-result.json) show seven more requests, including cross-book inventory 404, foreign-token posting 403, replacement approval and one recovered approved opening posting. Direct voucher INSERT, source occurrence UPDATE and source-financial INSERT privileges were absent from `openerp_app`.

To repeat the bounded flow, create a disposable PostgreSQL 17 database; apply migrations through `8380` with `bun run --cwd apps/api db:migrate 8380-sie-proposal-posting-guard.sql`; provision separate synthetic books/operators with unique scoped tokens and configure a local Wrangler Worker with restricted runtime database login and temporary Better Auth settings outside the checkout. Submit a synthetic SIE4 occurrence to `/source-occurrences`, preview, supply independent source account controls, seal/stage an immutable plan, select full-history basis, prepare and independently approve the source-ledger proposal, post through the owning financial run, then read `/sie-runs/:id/closing-comparison`. Repeat source/chunk/post with the **same** idempotency key and compare stable identities; use separate keys and stale fences only for negative probes. For an opening alternative, review/reprepare the opening proposal, require fresh approval and post only through `/historical-bases/:id/post`. The owning API contracts and [maintained SIE handoff](../../../apps/api/docs/SIE-HISTORICAL-IMPORT.md) give exact payloads. Never run this fixture against tenant data.

This proves only a synthetic slice, **not** full SIE exporter coverage, actual prior-system records, historical open-item truth, the D-04/D-06 decisions, hosted Worker behavior or statutory acceptance. One superseded-opening direct-execute probe was refused by the earlier approval guard, so it did not directly prove the later supersession guard. No concurrent crash scenario was exercised.
