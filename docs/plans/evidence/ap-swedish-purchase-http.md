# Reviewed supplier purchase HTTP observation

Observed 2026-09-24 with a **new disposable** PostgreSQL 17 database, restricted `openerp_app` role, synthetic SEK book and local Wrangler Worker. The [token-free HTTP request/response outline](ap-swedish-purchase-http.json) records submitted bodies, statuses, IDs and voucher lines. Rebuild IDs, digests, evidence and idempotency keys from each new response when repeating it. No provider was called.

## Repeat and results

1. Follow [local setup](../../local-development.md), migrate through `8760-supplier-line-vat.sql` and provision an isolated synthetic `synthetic-core-v1` SEK book with active BAS 2440 payable, 2641 input-VAT, 6000 expense and 1930 bank accounts. Run Wrangler with the restricted database login and a scoped operator token.
2. POST retained identity/original evidence, a supplier counterpart and an evidenced supplier draft with one `10000` net line, asserted tax `2500` and exact source gross `12500`. POST `/commerce/supplier-acceptance-reviews` using `swedish-purchase-v1`, a line assignment to expense 6000 and reviewed rate 25. Approve and execute the exact review digest. GET recognition voucher: expense debit 10000, input-VAT debit 2500, payable credit 12500.
3. POST a distinct credit source and `/commerce/supplier-credit-reviews` with `swedish-purchase-full-credit-v1`, amount 12500 and current invoice/acceptance values. Approve and execute its digest. GET voucher: payable debit 12500, expense credit 10000, input-VAT credit 2500. Outstanding becomes 0.
4. POST a second original with net 8000, asserted tax 2000, gross 10000 and a different supplier number/source evidence. Accept under the same reviewed line-rate profile. POST and separately approve a synthetic manual journal with payment evidence: payable debit 4000, bank credit 4000. POST `/commerce/allocation-plans` using its payable line and the second invoice, approve and apply. GET invoice: allocated 4000 and outstanding 6000.
5. Try a full credit for the second invoice after payment: `422 UnsupportedProfile`. The profile refuses paid-principal credits. Inspect voucher lines and counts in the JSON artifact; the disposable database retained two acceptances, one credit and one allocation receipt. Stop and remove only the processes/data created for this proof.

These are **synthetic-book** effects. The review still discloses `tax_profile_not_activated`; no actual company registration, source completeness, legal VAT deduction or bank-confirmed settlement was established. A full VAT-bearing credit is supported only for a wholly unpaid original; partial credits and refunds are not proved. The 4,000 payment was a separately posted synthetic journal, **not** the offline `pain.001` export or a reported provider state. No test files were added.
