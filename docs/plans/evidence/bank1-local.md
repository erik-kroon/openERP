# BANK-1 local synthetic verification — 2026-09-24

Scope: local PostgreSQL 17 and Cloudflare Worker, browser at `127.0.0.1:3107`, API at `127.0.0.1:18790`, synthetic book `book_customer_demo`. Source baseline `b23a98f`; the two BANK-1 corrections in the working tree were checked after that revision. This is not an actual bank feed, company acceptance, or a complete reconciled period.

## Observed path

- A synthetic posted debit of 250.00 SEK (`voucher_765bd7944cb54a4880c95a42005271a3`) joined an available 400.00 SEK posted debit on the 15,000.00 SEK customer-receipt observation. A third 550.00 SEK posted debit had no remaining capacity and was not selectable.
- One approved allocation, `bankplan_857caf6c1433430685ddc438e89e59d7`, saved **two legs** of 400.00 and 250.00 SEK. Its retained execution and the bank workspace both showed 13,800.00 SEK remaining on the observation; both posted lines had zero remaining. The account review reopened in the browser and displayed both legs. The review now shows the *whole plan's* residual on each leg, rather than misleading per-leg residuals.
- An approved whole-allocation reversal, `bankunmatch_7152b10a9c7a4301bdedf8719f18d9ed`, released both legs. The source returned to 14,450.00 SEK remaining; the bank ledger stayed 1,200.00 SEK. The original allocation and saved reversal remained visible after browser reload. Repeating reversal execution with the **same key** returned the same receipt, without a second release. The historical “after match” figures remain labelled as such after undo.
- The saved report `reconciliation_4ae53512d9ed437cbf4095093661282e` reopened after reload for account `account_bank`, 2026-09-23. It became stale after the new allocation. The account's difference on that closing date is now **bank minus ledger = +12,800.00 SEK**, the same direction as the saved report. The earlier account projection showed −12,800.00 SEK. Forward migration `8800-bank-workspace-difference-sign.sql` was applied locally and rerun without changing its migration receipt.
- Selecting 2026-09-24 in the browser cleared the report ID and hid the difference because the last statement ended on 2026-09-23. Before the statement, 2026-09-22 showed no statement balance or difference. Opening the saved report under that earlier period showed a scope warning rather than the wrong figures. An unmapped `account_receivables` URL returned a scoped 404 and did not show the saved report or old account amounts.

## Repeatable local inspection

The ignored `.cache/customer-frontend/bank1-proof.json` retains the synthetic IDs, exact request keys, full API before/after reads and reversal receipt. Browser captures are `bank1-multi-match.png`, `bank1-report.png`, `bank1-wrong-period.png`, `bank1-reversed.png` and `bank1-unmapped-account.png` in that directory. With the isolated preview from `.cache/customer-preview/runtime.json` running, `bun .cache/customer-frontend/bank1-reversal-browser.ts` repeats the final browser read/reload and account-scope inspection. The saved report and earlier active-match captures are historical snapshots; rerunning mutation drivers against the already reversed book is **not** a clean-state replay. Recreate the synthetic fixture on a fresh isolated book before replaying the write path.

`bun run --cwd apps/web check-types`, focused type-aware Oxlint on `bank-transaction-match.tsx`, and `git diff --check` passed. No test files or production data were changed. A second mapped bank account, real statement-source completeness, cross-book authorization, and a whole permitted-company period remain outside this local BANK-1 observation.
