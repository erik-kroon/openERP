# Wave 2 synthetic supplier payment HTTP proof

Observed 2026-09-24 with disposable PostgreSQL 17.11, local Wrangler Worker and two synthetic scoped operators. No provider or real-company data. The [structured observation and repeat outline](wave2-supplier-http/supplier-http-proof.json), [44 token-free HTTP request/response records](wave2-supplier-http/http-transcript.json), and [immutable offline XML](wave2-supplier-http/pain001-export.xml) are the verifiable artifacts. The XML SHA-256 is `15c07e8fe18539f90a401fc3e3c2a594074128c486376d91927b2b300b5616ab` (1,104 UTF-8 bytes). Its IBANs, names, and references are synthetic; do not submit it to a bank.

## Repeat and observed boundary

Use a **new** disposable cluster, `DATABASE_ADMIN_URL`, `bun run --cwd apps/api db:migrate`, the restricted `openerp_app` role, and a synthetic book provisioned as in [`docs/local-development.md`](../../local-development.md). Start the Worker with its `DATABASE_URL` pointing at that database, never an existing tenant. Follow the linked JSON's command outline and request transcript with newly generated evidence, invoice, operator, proposal, preview and idempotency identities. Exercise two distinct operator tokens to propose and independently verify each payee. GET eligibility without `after`; expect both registered accepted supplier invoices and their exact open amounts. Preview and export each payment with a current payee; read and retry using the **same** key, decode the exported bytes and compare their SHA-256. Report `unknown → reported_accepted → reported_settled` on the first export, and `reported_rejected` on the second; retry the exact outcome key and try forbidden contradictory/terminal transitions. Read the invoice/payment capacity and allocation receipts before stopping and deleting the disposable Worker and database. The committed XML is a result of this synthetic exercise, not a test fixture for production banking.

Before the fixes, omitted eligibility cursor returned HTTP `422`; the statement binding now passes SQL `NULL` for an empty cursor. Export returned `500` because JSON field extraction followed concatenation without parentheses; forward migration [`8710-supplier-payment-document.sql`](../../../apps/api/migrations/8710-supplier-payment-document.sql) corrects that SQL without changing a previously applied migration. In the fresh HTTP exercise, two accepted drafts became posted/register-bound invoices, with open amounts `12345` and `7890` minor units and allocation version `0`. The same-actor payee check failed `403`; independent review succeeded. Two exports retained exact bytes/hash, and same-key replay/read were identical. Reported acceptance could not regress to unknown or rejected (`409`), terminal outcomes stayed terminal, and a reported settlement left `bankVerified=false`, `paid=false`, `allocationCreated=false` with zero allocation receipts. The database retained two acceptances, two exports, four outcome rows and no payment allocation receipts.

This is not ISO XSD/provider certification, bank acceptance, account-owner verification, successful payment, or actual-company legal approval. D-04 tenant facts and D-10 provider/account/payment scheme credentials remain open.
