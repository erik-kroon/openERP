# Wave 2 fresh migration apply and replay

Observed 2026-09-24 at `ff93980` on a fresh disposable PostgreSQL 17 database. The repository had no pending worktree changes when the result was recorded. The maintenance command applied all **148** versioned SQL files, including `8370`, `8380`, `8390`, `8400`, `8500`, `8600` and `8710`. A second run returned `already applied` for every file. `SELECT count(*), min(name), max(name) FROM public.openerp_migrations` returned `148|0001-accounting.sql|8710-supplier-payment-document.sql`; the source directory also had 148 `.sql` files. First/second command stdout SHA-256: `1705659266a247c47f30c9aa420bc26c728a0e0551dad0f598ffe5741b191998` / `de8450cf399b84e881b05fda56df5ef2136bdc854cd35b9fe2d9aa802b20cf06`.

To repeat, create a new isolated PostgreSQL 17 cluster/database, set `DATABASE_ADMIN_URL` to that **disposable maintenance database**, run `bun run --cwd apps/api db:migrate` twice, then query `public.openerp_migrations`. Do not aim this command at a tenant database without the migration runbook and operator approval. The local database was destroyed afterward. This checks SQL apply/replay only; it does **not** validate SIE historical records, bank/provider operations, runtime-role access, actual-tenant balances, or legal acceptance.
