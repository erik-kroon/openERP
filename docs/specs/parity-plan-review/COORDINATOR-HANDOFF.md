# Coding-agent handoff: repair and use the parity plan

Read current AGENTS.md and the maintained core plans. This package reviews `ac9e1a918d86c6872f415e3a988cbc7d25b6f9fb`; recheck HEAD and dirty owner claims before editing. The local application-owned replacement is reported completed at a defined evidence scope. Do not restart SQL migration or take over unrelated active work because a historical paragraph says it is pending.

## First assignment: plan correction, not feature proliferation

Review and adopt `11-parity-backlog.REVISED.md`, `RULE-CORRECTIONS.md`, the 85-row `parity-register.json` and the proposed companion ADR edits. Keep PRY identifiers stable. Link every selected finding to its current canonical implementation owner. Do not generate 85 new independent services or merge these into the existing core denominator.

Before any financial implementation, remove the recipes that permit detector failure, missing-import plugs, ignored bank movements altering source totals, incorrect cash-method year-end handling, the sick-pay double deduction, circular VAT totals, incomplete Peppol arithmetic, inconsistent amendment receipts and omission of zero-net payroll accounting.

## Select work through the actual user workflow

Use the existing matched documents and current book requirements. Choose a finite scope that finishes recognition or settlement, produces a recoverable receipt and reconciles independent controls. Start native/file-based work without waiting for all provider integrations. Keep unsupported legal profiles explicit. Unattended auto-matching, additional payroll regimes and ROT/RUT are conditional extensions, not default tasks for every company.

An implementation card must specify the exact source/case, target operation, available profile inputs, pure financial calculation, refusal/ambiguity behavior, complete transactional effects, necessary owner handoffs and independent expected outcomes. Use the current Effect transaction and tx-passing persistence. No generic effect interpreter, provider platform rewrite or additional queue engine.

## Source evidence

Pin the actual reference before claiming a rule was preserved or verified. Do not substitute a historical reference commit from an old conversation for the unrecorded comparison source. Existing detailed plans count as requirement ownership; absence of complete runtime support does not make the model unowned. Discover current implementation with actual imports/callers and qualification evidence before rebuilding a capability.

## Financial and external execution

Current authorization, exact approved plan, relevant source/capacity versions and required complete checks are revalidated in the existing application-owned transaction. Journal and related domain effects, approval use and receipt commit together. Detection unavailable is a blocker to the affected financial action, not `CLEAR`. A chosen exact subset is still a candidate until its source/line identities and capacities are confirmed.

External calls and rendering occur outside financial locks. Provider idempotency and outcome semantics are real contracts, not guarantees manufactured by a local key. Output artifacts retain exact bytes and separate prepared/validated/signed/submitted/accepted states. Rules and schemas are selected by their applicability, not today's newest version.

## Verification and delivery

Follow the actual authorization for code, tests and runtime work. This document adds no production-data, payment, filing or test permission. Run the existing planning checks for their stated scope. Record separately the supplemental structure check, independent arithmetic results and actual permitted application/database/provider observations.

Return the precise changed source/doc paths, selected finding IDs, retained owner interfaces, risk fixes, checks actually performed and remaining inputs. Do not claim complete parity from a passing document validator or a changing task count. Keep failed cases and unsupported portions visible.
